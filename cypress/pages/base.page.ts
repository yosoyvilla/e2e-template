import _ = require('lodash');
export class BasePage  {
    // Common methods for Page Object
    protected url = '';

    async getUrl(urlParam: string) {
        return await this.getUrl(urlParam);
    }
}
